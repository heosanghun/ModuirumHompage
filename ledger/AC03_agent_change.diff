--- /home/sims/auto/atom-company/agent.py.ac02.bak	2026-09-18 23:09:12.448666517 +0900
+++ /home/sims/auto/atom-company/agent.py	2026-09-18 23:09:44.525610141 +0900
@@ -19,6 +19,67 @@
         self.reason = reason
         self.details = details
 
+# AC-03 Task F: Ollama /api/chat Native Tools Schema (OpenAI compatible)
+TOOLS = [
+    {
+        "type": "function",
+        "function": {
+            "name": "write_file",
+            "description": "Write text content to a file strictly within workspace directory",
+            "parameters": {
+                "type": "object",
+                "properties": {
+                    "path": {"type": "string", "description": "Relative path to file inside workspace"},
+                    "content": {"type": "string", "description": "Text content to write"}
+                },
+                "required": ["path", "content"]
+            }
+        }
+    },
+    {
+        "type": "function",
+        "function": {
+            "name": "read_file",
+            "description": "Read text content of a file strictly within workspace directory",
+            "parameters": {
+                "type": "object",
+                "properties": {
+                    "path": {"type": "string", "description": "Relative path to file inside workspace"}
+                },
+                "required": ["path"]
+            }
+        }
+    },
+    {
+        "type": "function",
+        "function": {
+            "name": "list_dir",
+            "description": "List files and subdirectories in a workspace directory",
+            "parameters": {
+                "type": "object",
+                "properties": {
+                    "path": {"type": "string", "description": "Relative path inside workspace (empty for workspace root)"}
+                },
+                "required": []
+            }
+        }
+    },
+    {
+        "type": "function",
+        "function": {
+            "name": "run_shell",
+            "description": "Execute a shell command with CWD restricted to workspace directory",
+            "parameters": {
+                "type": "object",
+                "properties": {
+                    "cmd": {"type": "string", "description": "Shell command to execute in workspace"}
+                },
+                "required": ["cmd"]
+            }
+        }
+    }
+]
+
 class AgentRuntime:
     def __init__(self, role, model="gemma4:e4b", backend="ollama", root="/home/sims/auto/atom-company", poll_interval=1.0):
         self.role = role.upper()
@@ -48,6 +109,7 @@
         self.msg_counter = 0
         self.consecutive_5xx = 0
         self.current_task_id = "TASK-0001"
+        self.retry_count = {}  # AC-03 Task H: Per-message retry tracking
 
     def log(self, msg_id, from_role, to_role, task_id, msg_type, tool_count, latency_ms, status, note=""):
         ts = datetime.datetime.now(datetime.timezone.utc).isoformat()
@@ -62,34 +124,40 @@
             f.write(f"[{ts}] {text}\n")
 
     # ==========================================
-    # WORKSPACE RESTRICTED TOOLS & HUMAN_GATE
+    # AC-03 Task I: SHELL & WORKSPACE SECURITY
     # ==========================================
     def _check_security(self, action, target_path=None, cmd=None):
+        net_pattern = re.compile(r"\b(curl|wget|nc|ssh|ping|nmap|git\s+(clone|push)|pip\s+install|npm\s+install)\b", re.IGNORECASE)
+        audio_pattern = re.compile(r"\b(aplay|paplay|speaker-test|beep)\b", re.IGNORECASE)
+        billing_pattern = re.compile(r"\b(stripe|billing|credit_card|purchase|subscribe|buy_tokens)\b", re.IGNORECASE)
+
         if cmd:
-            cmd_lower = cmd.lower()
-            net_keywords = ["curl", "wget", "nc ", "ssh ", "ping ", "nmap", "ftp ", "sftp ", "telnet", "git clone", "git push", "pip install", "npm install", "docker pull"]
-            for kw in net_keywords:
-                if kw in cmd_lower:
-                    raise HumanGateViolation("EXTERNAL_NETWORK_ATTEMPT", f"Command contains network pattern: '{kw}'")
-                    
-            billing_keywords = ["stripe", "pay ", "billing", "credit_card", "purchase", "subscribe", "buy_tokens"]
-            for kw in billing_keywords:
-                if kw in cmd_lower:
-                    raise HumanGateViolation("FINANCIAL_CHARGE_ATTEMPT", f"Command contains billing keyword: '{kw}'")
-
-            audio_keywords = ["aplay", "paplay", "speaker-test", "beep", "play -n", "alsa", "pulseaudio", "sound"]
-            for kw in audio_keywords:
-                if kw in cmd_lower:
-                    raise HumanGateViolation("AUDIO_ALERT_ATTEMPT", f"Command contains audio keyword: '{kw}'")
+            if net_pattern.search(cmd):
+                raise HumanGateViolation("EXTERNAL_NETWORK_ATTEMPT", f"Command contains network keyword: '{cmd}'")
+            if audio_pattern.search(cmd):
+                raise HumanGateViolation("AUDIO_ALERT_ATTEMPT", f"Command contains audio keyword: '{cmd}'")
+            if billing_pattern.search(cmd):
+                raise HumanGateViolation("FINANCIAL_CHARGE_ATTEMPT", f"Command contains billing keyword: '{cmd}'")
+
+            # Shell path escape checks: absolute paths (except /dev/null and python/shell paths), .., cd , ~
+            allowed_prefixes = ["/dev/null", sys.executable, "/usr/bin/python3", "/bin/sh", "/bin/bash"]
+            tokens = cmd.split()
+            for t in tokens:
+                clean_t = t.strip("\"'<>|;&")
+                if clean_t.startswith("/") and not any(clean_t == p or clean_t.startswith(p) for p in allowed_prefixes):
+                    raise HumanGateViolation("OUT_OF_WORKSPACE_SHELL", f"Command contains unauthorized absolute path: '{clean_t}' in '{cmd}'")
+            if ".." in cmd or "cd " in cmd or "~" in cmd:
+                raise HumanGateViolation("OUT_OF_WORKSPACE_SHELL", f"Command contains workspace escape pattern: '{cmd}'")
 
         if target_path:
-            norm = os.path.normpath(os.path.join(self.workspace_dir, target_path))
-            if not norm.startswith(os.path.abspath(self.workspace_dir)):
+            norm = os.path.realpath(os.path.join(self.workspace_dir, target_path))
+            ws = os.path.realpath(self.workspace_dir)
+            if not (norm == ws or norm.startswith(ws + os.sep)):
                 raise HumanGateViolation("OUT_OF_WORKSPACE_PATH", f"Attempted access outside workspace: '{target_path}' -> '{norm}'")
 
     def tool_read_file(self, rel_path):
         self._check_security("read_file", target_path=rel_path)
-        full = os.path.normpath(os.path.join(self.workspace_dir, rel_path))
+        full = os.path.realpath(os.path.join(self.workspace_dir, rel_path))
         if not os.path.exists(full):
             return f"ERROR: File not found: {rel_path}"
         with open(full, "r", encoding="utf-8") as f:
@@ -99,17 +167,17 @@
 
     def tool_write_file(self, rel_path, content):
         self._check_security("write_file", target_path=rel_path)
-        full = os.path.normpath(os.path.join(self.workspace_dir, rel_path))
+        full = os.path.realpath(os.path.join(self.workspace_dir, rel_path))
         os.makedirs(os.path.dirname(full), exist_ok=True)
         with open(full, "w", encoding="utf-8") as f:
             f.write(content)
-        # AC-02 B-2: Explicit tool logging for deliverable attribution audit
+        # AC-02 B-2 / AC-03 Task F: Explicit tool logging for deliverable attribution audit
         self.log(f"TOOL-WRITE-{int(time.time()*1000)%1000000}", self.role, "WORKSPACE", self.current_task_id, "tool_call", 1, 0.0, "EXECUTED", f"tool=write_file path={rel_path}")
         return f"SUCCESS: Wrote {len(content)} bytes to {rel_path}"
 
     def tool_list_dir(self, rel_path=""):
         self._check_security("list_dir", target_path=rel_path)
-        full = os.path.normpath(os.path.join(self.workspace_dir, rel_path))
+        full = os.path.realpath(os.path.join(self.workspace_dir, rel_path))
         if not os.path.exists(full):
             return f"ERROR: Directory not found: {rel_path}"
         items = os.listdir(full)
@@ -127,6 +195,18 @@
         except Exception as e:
             return f"ERROR: Shell execution failed: {str(e)}"
 
+    def _dispatch_tool(self, name, args):
+        if name == "write_file":
+            return self.tool_write_file(args.get("path", ""), args.get("content", ""))
+        elif name == "read_file":
+            return self.tool_read_file(args.get("path", ""))
+        elif name == "list_dir":
+            return self.tool_list_dir(args.get("path", ""))
+        elif name == "run_shell":
+            return self.tool_run_shell(args.get("cmd", ""))
+        else:
+            return f"ERROR: Unknown tool '{name}'"
+
     def raise_human_gate_request(self, task_id, violation_type, detail):
         ts = datetime.datetime.now(datetime.timezone.utc).strftime("%Y%m%d-%H%M%S")
         req_id = f"REQ-{ts}-{violation_type[:10]}"
@@ -178,7 +258,7 @@
         done_files = sorted(os.listdir(self.done_dir))
         task_msgs = []
         for fn in done_files:
-            if not fn.endswith(".md"):
+            if not fn.endswith(".md") or fn.startswith("DEADLETTER_"):
                 continue
             fp = os.path.join(self.done_dir, fn)
             try:
@@ -217,7 +297,7 @@
                 f.write(content)
 
     def _mark_task_done_by_pm(self, task_id, decision_id):
-        # AC-02 B-1: Task card status is strictly modified by PM agent decision
+        # AC-02 B-1 / AC-03 G: Task card status is strictly modified by PM agent decision
         card_path = os.path.join(self.tasks_dir, f"{task_id}.md")
         if os.path.exists(card_path):
             with open(card_path, "r", encoding="utf-8") as f:
@@ -229,48 +309,69 @@
             self.log(decision_id, "PM", "TASKS", task_id, "decision", 0, 0.0, "TASK_MARKED_DONE", f"card={task_id}.md")
 
     # ==========================================
-    # GUARD 2: CONTEXT BUDGET (24K limit)
+    # AC-03 Task J: REAL CONTEXT & REAL SUMMARY
     # ==========================================
-    def check_and_compress_context(self, task_id, system_prompt, task_card, recent_messages, token_limit=24000):
-        combined_text = system_prompt + "\n" + task_card + "\n" + "\n".join(recent_messages)
+    def _load_task_history(self, task_id, n=10):
+        msgs = []
+        if os.path.exists(self.done_dir):
+            for fn in os.listdir(self.done_dir):
+                if not fn.endswith(".md") or fn.startswith("DEADLETTER_") or fn.startswith("REJECTED_") or fn.startswith("VIOLATION_"):
+                    continue
+                fp = os.path.join(self.done_dir, fn)
+                try:
+                    with open(fp, "r", encoding="utf-8") as f:
+                        meta, body = parse_frontmatter(f.read())
+                        if meta and meta.get("task") == task_id:
+                            msgs.append({
+                                "id": meta.get("id"),
+                                "from": meta.get("from"),
+                                "to": meta.get("to"),
+                                "type": meta.get("type"),
+                                "created": meta.get("created", ""),
+                                "body": body
+                            })
+                except Exception:
+                    continue
+        msgs.sort(key=lambda x: x["created"])
+        return msgs[-n:]
+
+    def check_and_compress_context(self, task_id, system_prompt, task_card, recent_msgs, token_limit=24000):
+        lines = [f"[{m['from']} ({m['id']})]: {m['body']}" for m in recent_msgs]
+        combined_text = system_prompt + "\n" + task_card + "\n" + "\n".join(lines)
         est_tokens = int(len(combined_text) / 3.5)
 
         if est_tokens > token_limit:
-            self.write_guard_log("budget", f"BUDGET_EXCEEDED: est_tokens={est_tokens} > limit={token_limit} for {task_id}. Triggering summary compression.")
+            self.write_guard_log("budget", f"BUDGET_EXCEEDED: est_tokens={est_tokens} > limit={token_limit} for {task_id}. Compressing history.")
+            # AC-03 Task J: No fake sentences! Truncate older messages by ID + first 300 chars
+            summary_lines = [f"- {m['id']} ({m['from']}, {m['type']}): {m['body'][:300].strip()}…" for m in recent_msgs]
+            summary_content = "# Compressed Task History (Truncated)\n" + "\n".join(summary_lines)
             summary_path = os.path.join(self.ledger_dir, f"summary_{task_id}.md")
-            summary_content = f"""# Compact Summary for {task_id}
-- Generated: {datetime.datetime.now(datetime.timezone.utc).isoformat()}
-- Original Messages Compressed: {len(recent_messages)}
-- Summary: Engineering deliverables implemented and tested. Verification ongoing.
-"""
             with open(summary_path, "w", encoding="utf-8") as f:
                 f.write(summary_content)
-            self.write_guard_log("budget", f"Summary written to {summary_path}. Injected compact summary.")
-            return f"[SUMMARY INJECTED from {summary_path}]\n" + summary_content
-        return "\n".join(recent_messages)
+            self.write_guard_log("budget", f"Truncated history written to {summary_path}.")
+            return summary_content
+        return "\n".join(lines)
 
     # ==========================================
-    # GUARD 3: WATCHDOG & BACKOFF
+    # AC-03 Task F: OLLAMA /API/CHAT & TOOL LOOP
     # ==========================================
-    def call_llm_with_watchdog(self, prompt, timeout=60, task_id="TASK-0001", step_idx=1):
-        start_t = time.time()
-        if self.backend == "mock":
-            return self._mock_llm_response(prompt, task_id=task_id, step_idx=step_idx)
-
+    def _post_chat(self, messages, tools=None, timeout=60):
         url = "http://127.0.0.1:11434/api/chat"
         payload = {
             "model": self.model,
-            "messages": [{"role": "user", "content": prompt}],
+            "messages": messages,
             "stream": False
         }
+        if tools:
+            payload["tools"] = tools
+
         data = json.dumps(payload).encode("utf-8")
         req = urllib.request.Request(url, data=data, headers={"Content-Type": "application/json"})
 
         try:
             with urllib.request.urlopen(req, timeout=timeout) as resp:
                 self.consecutive_5xx = 0
-                res_obj = json.loads(resp.read().decode("utf-8"))
-                return res_obj.get("message", {}).get("content", "")
+                return json.loads(resp.read().decode("utf-8"))
         except urllib.error.HTTPError as e:
             if e.code >= 500:
                 self.consecutive_5xx += 1
@@ -279,25 +380,57 @@
                     self.write_guard_log("watchdog", "Ollama 5xx 3 consecutive times! Entering 30s exponential backoff.")
                     time.sleep(30)
             raise e
+
+    def call_llm_with_watchdog(self, role_prompt, task_card, history_text, task_id="TASK-0001", timeout=60):
+        start_t = time.time()
+        if self.backend == "mock":
+            # Mock mode for tests/test_protocol.py
+            return self._mock_llm_response(role_prompt, task_card, history_text, task_id=task_id)
+
+        # Real Ollama Native Tools Loop (Max 8 tool calls per message)
+        messages = [
+            {"role": "system", "content": role_prompt},
+            {"role": "user", "content": f"[TASK CARD]\n{task_card}\n\n[CONVERSATION HISTORY]\n{history_text}"}
+        ]
+        tool_count = 0
+
+        try:
+            for _ in range(8):
+                res = self._post_chat(messages, tools=TOOLS, timeout=timeout)
+                msg = res.get("message", {})
+                messages.append(msg)
+                calls = msg.get("tool_calls") or []
+
+                if not calls:
+                    return msg.get("content", ""), tool_count
+
+                for c in calls:
+                    fn = c.get("function", {}).get("name", "")
+                    raw_args = c.get("function", {}).get("arguments", {})
+                    args = json.loads(raw_args) if isinstance(raw_args, str) else raw_args
+                    out = self._dispatch_tool(fn, args)  # HumanGateViolation will propagate cleanly
+                    messages.append({"role": "tool", "content": str(out)})
+                    tool_count += 1
+
+            return "[TOOL_LIMIT_REACHED]", tool_count
+
         except Exception as e:
             elapsed = time.time() - start_t
             if elapsed >= timeout:
                 self.write_guard_log("watchdog", f"WATCHDOG: LLM call timed out after {elapsed:.1f}s >= {timeout}s.")
             raise e
 
-    def _mock_llm_response(self, prompt, task_id="TASK-0001", step_idx=1):
-        # Isolated mock response engine for tests/test_protocol.py
+    def _mock_llm_response(self, role_prompt, task_card, history_text, task_id="TASK-0001"):
+        # Isolated mock response logic for tests/test_protocol.py
         if self.role == "PM":
-            if "report" in prompt or "Ran" in prompt:
-                return f"[PM DECISION] Task {task_id}: Verification accepted. Deliverables confirmed. Task status marked done."
-            return f"[PM ASSIGN] Task {task_id}: Please implement requirement in workspace/ and execute test_todo.py."
+            if "EXIT: 0" in history_text or "EXIT:  0" in history_text:
+                return f"[PM REVIEW] Task {task_id}: Deliverables and tests verified.\nDECISION: ACCEPT"
+            elif "REJECT" in history_text:
+                return f"[PM REVIEW] Task {task_id}: Requirements not met.\nDECISION: REJECT"
+            else:
+                return f"[PM ASSIGN] Task {task_id}: Please implement deliverables in workspace/ and run tests.\nDECISION: CONTINUE"
         else: # ENG
-            # ENG calls tool_write_file legitimately if not existing
-            todo_p = os.path.join(self.workspace_dir, "todo_cli.py")
-            if not os.path.exists(todo_p):
-                self.tool_write_file("todo_cli.py", "#!/usr/bin/env python3\nimport sys\nprint('todo_cli')\n")
-            test_out = self.tool_run_shell("python3 -c 'print(\"Ran 6 tests. OK\")'")
-            return f"[ENG REPORT] Task {task_id}: Work executed. Test result: {test_out}"
+            return f"[ENG REPORT] Task {task_id}: Executed testing in workspace.\nEXIT: 0\nSTDOUT: Ran tests OK."
 
     # ==========================================
     # INBOX POLLING & MESSAGE PROCESSING
@@ -310,6 +443,7 @@
         msg_file = inbox_files[0]
         msg_path = os.path.join(self.inbox_dir, msg_file)
         start_time = time.time()
+        task_id = "TASK-0001"
 
         try:
             with open(msg_path, "r", encoding="utf-8") as f:
@@ -333,7 +467,7 @@
                 os.replace(msg_path, os.path.join(self.done_dir, f"BLOCKED_{msg_file}"))
                 return True
 
-            # 2. Build Prompt
+            # 2. Build Prompt with real history (AC-03 Task J)
             role_prompt_path = os.path.join(self.roles_dir, f"{self.role}.md")
             role_prompt = ""
             if os.path.exists(role_prompt_path):
@@ -346,22 +480,37 @@
                 with open(task_card_path, "r", encoding="utf-8") as f:
                     task_card = f.read()
 
-            recent_msgs = [f"[{meta.get('from')}]: {body}"]
-            history = self.check_and_compress_context(task_id, role_prompt, task_card, recent_msgs)
-
-            full_prompt = f"""[SYSTEM ROLE PROMPT]\n{role_prompt}\n\n[TASK CARD]\n{task_card}\n\n[CONVERSATION HISTORY]\n{history}"""
-
-            # 3. Call LLM
-            step_idx = self.msg_counter + 1
-            response_text = self.call_llm_with_watchdog(full_prompt, task_id=task_id, step_idx=step_idx)
+            history_msgs = self._load_task_history(task_id, n=10)
+            history_msgs.append({
+                "id": meta.get("id"),
+                "from": meta.get("from"),
+                "to": meta.get("to"),
+                "type": meta.get("type"),
+                "created": meta.get("created", ""),
+                "body": body
+            })
+            history_text = self.check_and_compress_context(task_id, role_prompt, task_card, history_msgs)
+
+            # 3. Call LLM (with Native Tools Loop)
+            response_tuple = self.call_llm_with_watchdog(role_prompt, task_card, history_text, task_id=task_id)
+            if isinstance(response_tuple, tuple):
+                response_text, tool_calls_count = response_tuple
+            else:
+                response_text, tool_calls_count = response_tuple, 0
 
-            # 4. Determine response type & PM decision handling
+            # 4. AC-03 Task G: Structured PM Decision handling
             if self.role == "PM":
-                is_done_decision = "done" in response_text.lower() or "accepted" in response_text.lower()
-                resp_type = "decision" if is_done_decision else "assign"
+                m = re.search(r"^DECISION:\s*(ACCEPT|REJECT|CONTINUE)\s*$", response_text, re.M)
+                verdict = m.group(1) if m else "CONTINUE"
+                # Acceptance strictly requires report with EXIT: 0
+                is_done_decision = (
+                    verdict == "ACCEPT"
+                    and meta.get("type") == "report"
+                    and re.search(r"EXIT:\s*0", body) is not None
+                )
+                resp_type = {"ACCEPT": "decision", "REJECT": "review", "CONTINUE": "assign"}[verdict]
                 out_id, out_path = self.send_message(self.peer_role, in_reply_to, task_id, resp_type, response_text)
                 if is_done_decision:
-                    # PM updates task card status to done (AC-02 Task B-1)
                     self._mark_task_done_by_pm(task_id, out_id)
             else:
                 resp_type = "report"
@@ -372,16 +521,25 @@
             os.replace(msg_path, done_dest)
 
             latency_ms = (time.time() - start_time) * 1000.0
-            self.log(out_id, self.role, self.peer_role, task_id, resp_type, 1, latency_ms, "PROCESSED", f"in_reply_to={in_reply_to}")
+            self.log(out_id, self.role, self.peer_role, task_id, resp_type, tool_calls_count, latency_ms, "PROCESSED", f"in_reply_to={in_reply_to}")
             return True
 
         except HumanGateViolation as hgv:
-            self.raise_human_gate_request(meta.get("task", "TASK-0001"), hgv.reason, hgv.details)
-            self.log(msg_file, self.role, self.peer_role, meta.get("task", "UNKNOWN"), "humangate", 0, 0, "BLOCKED", hgv.reason)
+            self.raise_human_gate_request(task_id, hgv.reason, hgv.details)
+            self.log(msg_file, self.role, self.peer_role, task_id, "humangate", 0, 0.0, "BLOCKED", hgv.reason)
             os.replace(msg_path, os.path.join(self.done_dir, f"VIOLATION_{msg_file}"))
             return True
         except Exception as e:
-            self.log(msg_file, "SYSTEM", self.role, "UNKNOWN", "error", 0, 0, "ERROR", str(e))
+            # AC-03 Task H: Retry limit & Deadletter handling
+            n = self.retry_count.get(msg_file, 0) + 1
+            self.retry_count[msg_file] = n
+            self.log(msg_file, "SYSTEM", self.role, task_id, "error", 0, 0.0, "ERROR", f"retry={n} {e}")
+            if n >= 3:
+                deadletter_dest = os.path.join(self.done_dir, f"DEADLETTER_{msg_file}")
+                os.replace(msg_path, deadletter_dest)
+                self._mark_task_blocked(task_id, f"deadletter after {n} retries: {e}")
+                self.raise_human_gate_request(task_id, "DEADLETTER", str(e))
+                self.write_guard_log("watchdog", f"DEADLETTER {msg_file} task={task_id} retry={n} error={e}")
             return False
 
     def run_loop(self, max_iterations=None):
